# Install Local LLM

This app uses a local LLM for transcription by default.

## Local LLM (LM Studio)

1. Install LM Studio.
2. Download and run `qwen/qwen3-vl-8b`.
3. Start the local server at `http://127.0.0.1:1234`.
